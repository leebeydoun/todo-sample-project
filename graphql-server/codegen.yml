overwrite: true
schema: "./api/TodoSchema.graphql"
documents: null
generates:
  ./api/generated/graphql.ts:
    plugins:
      - "typescript"
      - "typescript-resolvers"
  ./graphql.schema.json:
    plugins:
      - "introspection"
